# MT5 Simulator Requirements
# =========================

# Core dependencies
numpy>=1.21.0
pandas>=1.3.0
matplotlib>=3.5.0
tkinter  # Built-in with Python

# Optional dependencies for better performance
scipy>=1.7.0
seaborn>=0.11.0

# Note: tkinter is usually included with Python installation
# If not available, install with: sudo apt-get install python3-tk (Ubuntu/Debian)
# or: brew install python-tk (macOS)
